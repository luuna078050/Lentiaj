const Voice={recognition:null,supported:false,init(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return;this.supported=true;this.recognition=new SR();this.recognition.lang='ru-RU';this.recognition.interimResults=false;this.recognition.maxAlternatives=1;},start(onResult,onEnd){if(!this.supported){toast('Голосовой ввод недоступен. Можно использовать кнопку Сканировать.');return}this.recognition.onresult=e=>onResult(e.results[0][0].transcript);this.recognition.onend=()=>onEnd?.();this.recognition.onerror=()=>toast('Не расслышал. Попробуй ещё раз.');this.recognition.start();toast('Слушаю…');},speak(text){if('speechSynthesis' in window){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='ru-RU';u.rate=.96;speechSynthesis.speak(u);}}};Voice.init();
