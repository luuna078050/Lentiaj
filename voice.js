const Voice={recognition:null,supported:false,lang:'ru-RU',init(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return;this.supported=true;this.recognition=new SR();this.recognition.interimResults=false;this.recognition.maxAlternatives=1;this.setLanguage(window.LANGS?.[window.currentLanguage?.()||'ru']?.voice||'ru-RU')},setLanguage(lang){this.lang=lang;if(this.recognition)this.recognition.lang=lang},start(onResult,onEnd){if(!this.supported){toast(t('voiceoff'));return}this.recognition.lang=this.lang;this.recognition.onresult=e=>onResult(e.results[0][0].transcript);this.recognition.onend=()=>onEnd?.();this.recognition.onerror=()=>toast(t('notheard'));try{this.recognition.start();toast(t('listening'))}catch{}},speak(text){if('speechSynthesis'in window){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=this.lang;u.rate=.96;speechSynthesis.speak(u)}}};Voice.init();